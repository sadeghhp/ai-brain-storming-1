{
  "name": "ai-brainstorm",
  "version": "1.0.0",
  "description": "Multi-agent AI brainstorming playground",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "dexie": "^4.0.10",
    "uuid": "^11.0.3",
    "gpt-tokenizer": "^2.5.2"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.6",
    "vitest": "^2.1.0",
    "@types/uuid": "^10.0.0"
  }
}

